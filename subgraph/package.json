{
  "name": "prime-desk-subgraph",
  "version": "1.0.0",
  "scripts": {
    "prepare-manifest": "node prepare.mjs",
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --studio prime-desk"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.69.0",
    "@graphprotocol/graph-ts": "^0.35.1"
  }
}
